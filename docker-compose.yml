services:
  app:
    build:
      context: ./COMPASS
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
        NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
        NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
        # Build-time metadata for /api/health. Coolify env vault sets these via
        # GH Actions deploy.yml on every push to main (see PR #2).
        GIT_SHA: ${GIT_SHA:-unknown}
        BUILT_AT: ${BUILT_AT:-unknown}
        # Sentry build-time args. DSN is public (browser bundle); auth token is
        # build-only and stripped before runtime image. Both come from Coolify
        # env vault with is_buildtime=true.
        NEXT_PUBLIC_SENTRY_DSN: ${NEXT_PUBLIC_SENTRY_DSN:-}
        NEXT_PUBLIC_GIT_SHA: ${GIT_SHA:-unknown}
        SENTRY_AUTH_TOKEN: ${SENTRY_AUTH_TOKEN:-}
    image: compass-app:latest
    container_name: compass-app
    restart: unless-stopped
    ports:
      - "${APP_PORT:-10000}:10000"
    environment:
      NODE_ENV: production
      PORT: 10000
      NEXT_PUBLIC_SUPABASE_URL: ${NEXT_PUBLIC_SUPABASE_URL}
      NEXT_PUBLIC_SUPABASE_ANON_KEY: ${NEXT_PUBLIC_SUPABASE_ANON_KEY}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL}
      NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-${NEXT_PUBLIC_APP_URL}}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      CRON_SECRET: ${CRON_SECRET}
      SUPER_ADMIN_EMAILS: ${SUPER_ADMIN_EMAILS}
      # Consultant Success rollout flags. The private surface is enabled while
      # automations stay non-delivering in shadow mode and surveys stay disabled.
      CONSULTANT_SUCCESS_ENABLED: ${CONSULTANT_SUCCESS_ENABLED:-true}
      CONSULTANT_SUCCESS_AUTOMATIONS_ENABLED: ${CONSULTANT_SUCCESS_AUTOMATIONS_ENABLED:-true}
      CONSULTANT_SUCCESS_SURVEYS_ENABLED: ${CONSULTANT_SUCCESS_SURVEYS_ENABLED:-false}
      CONSULTANT_SUCCESS_SHADOW_MODE: ${CONSULTANT_SUCCESS_SHADOW_MODE:-true}
      CONSULTANT_SUCCESS_TOKEN_SECRET: ${CONSULTANT_SUCCESS_TOKEN_SECRET:-}
      # Sentry runtime envs (server-side, NOT NEXT_PUBLIC_). DSN unset = SDK no-op.
      SENTRY_DSN: ${SENTRY_DSN:-}
      SENTRY_ENVIRONMENT: ${SENTRY_ENVIRONMENT:-production}
      SENTRY_TRACES_SAMPLE_RATE: ${SENTRY_TRACES_SAMPLE_RATE:-0.1}
      SENTRY_PROFILES_SAMPLE_RATE: ${SENTRY_PROFILES_SAMPLE_RATE:-0.1}
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://127.0.0.1:10000/api/health"]
      interval: 30s
      timeout: 10s
      start_period: 40s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"
        # Tag is consumed by Grafana Alloy (PR #10) as `service`/`app` labels in Loki.
        tag: "{{.ImageName}}/{{.Name}}/{{.ID}}"

  # Grafana Alloy log shipper. Profiled so the service only starts when
  # Coolify (or local dev) sets COMPOSE_PROFILES=observability — keeps
  # this PR safe to merge before Grafana Cloud credentials are provisioned
  # in the env vault. Once GRAFANA_LOKI_* are set, flip the profile and
  # logs start flowing.
  alloy:
    image: grafana/alloy:latest
    container_name: compass-alloy
    restart: unless-stopped
    command:
      - run
      - --server.http.listen-addr=0.0.0.0:12345
      - /etc/alloy/config.alloy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./alloy/config.alloy:/etc/alloy/config.alloy:ro
    environment:
      GRAFANA_LOKI_URL: ${GRAFANA_LOKI_URL}
      GRAFANA_LOKI_USER: ${GRAFANA_LOKI_USER}
      GRAFANA_LOKI_TOKEN: ${GRAFANA_LOKI_TOKEN}
      HOSTNAME: ${ALLOY_HOSTNAME:-compass-prod}
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "2"
