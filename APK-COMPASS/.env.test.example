# =================================================================
# COMPASS — environment for E2E tests + automation scripts
# Copy to .env.test (gitignored) and fill in real values.
# =================================================================

# --- Hetzner staging URL ---
BASE_URL=https://compass.dynaminds.pl
PLAYWRIGHT_BASE_URL=https://compass.dynaminds.pl

# --- Supabase staging (same project as Hetzner staging app) ---
NEXT_PUBLIC_SUPABASE_URL=https://xxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...
# CRITICAL: service role key — needed to create/promote test users + cleanup
# Source: Supabase Dashboard → Project Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# --- 4 test accounts (created automatically by scripts/setup-test-users.ts) ---
# All emails on @b2bnetwork.pl with plus-aliases. Same password for all.
TEST_PASSWORD=ComPass-2026-Test!

TEST_CONSULTANT_EMAIL=e2e+consultant@b2bnetwork.pl
TEST_ADMIN_EMAIL=e2e+admin@b2bnetwork.pl
TEST_CENTRALA_EMAIL=e2e+centrala@b2bnetwork.pl
TEST_ADMINISTRATOR_EMAIL=e2e+administrator@b2bnetwork.pl

# --- Optional: real AI keys for the eval harness only (test:eval) ---
# ANTHROPIC_API_KEY=sk-ant-...
# VOYAGE_API_KEY=pau-...
